# Epic: LoomGraph 集成支持

**Epic ID**: EPIC-001
**版本**: 0.1.0
**创建日期**: 2025-02-04
**状态**: 📋 规划中

---

## 背景

LoomGraph 是代码智能引擎，需要将代码结构（Symbol, Call, Inheritance）注入到 LightRAG 进行存储和检索。

**集成方**: LoomGraph (github.com/user/LoomGraph)
**需求文档**: `LoomGraph/docs/integration/LIGHTRAG_REQUIREMENTS.md`

---

## MVP 评估结果

| API | 状态 | 说明 |
|-----|------|------|
| `acreate_entity()` | ✅ 已有 | 满足需求 |
| `acreate_relation()` | ✅ 已有 | 满足需求 |
| `aquery()` | ✅ 已有 | 满足需求 |
| `get_node_edges()` | ✅ 已有 | 满足需求 |
| `remove_nodes()` | ✅ 已有 | 满足需求 |
| `adelete_by_entity()` | ✅ 已有 | 满足需求 |

**结论**: MVP 阶段 LightRAG 无需改动，现有 API 完全满足需求。

---

## Feature 列表

### Feature 1: MVP 集成验证 (v1.5.0)

**优先级**: P0 - 必须
**状态**: ✅ 已完成

验证现有 API 满足 LoomGraph 集成需求。

#### Stories

| Story ID | 标题 | 估点 | 状态 |
|----------|------|------|------|
| S1.1 | 编写 LoomGraph 集成示例代码 | 2 | ✅ |
| S1.2 | 编写集成测试用例 | 3 | ✅ |
| S1.3 | 更新 API 文档说明 | 1 | 🔲 |

---

### Feature 2: Upsert 语义支持 (v1.6.0)

**优先级**: P1 - 重要
**状态**: 🔲 待开始

为 `acreate_entity()` 和 `acreate_relation()` 添加 upsert 支持，简化增量更新场景。

#### Stories

| Story ID | 标题 | 估点 | 状态 |
|----------|------|------|------|
| S2.1 | 设计 upsert API 接口 | 2 | 🔲 |
| S2.2 | 实现 `aupsert_entity()` | 3 | 🔲 |
| S2.3 | 实现 `aupsert_relation()` | 3 | 🔲 |
| S2.4 | 编写单元测试 | 2 | 🔲 |
| S2.5 | 编写集成测试 | 2 | 🔲 |

---

### Feature 3: 按文件批量删除 (v1.6.0)

**优先级**: P1 - 重要
**状态**: 🔲 待开始

支持按 `file_path` 或 `source_id` 前缀批量删除 entities，简化 Warm Update 场景。

#### Stories

| Story ID | 标题 | 估点 | 状态 |
|----------|------|------|------|
| S3.1 | 设计批量删除 API | 2 | 🔲 |
| S3.2 | 实现 `adelete_entities_by_file()` | 3 | 🔲 |
| S3.3 | 编写测试用例 | 2 | 🔲 |

---

### Feature 4: 边类型过滤 (v1.7.0)

**优先级**: P2 - 一般
**状态**: 🔲 待开始

为 `get_node_edges()` 添加按 `keywords` (relation_type) 过滤功能。

#### Stories

| Story ID | 标题 | 估点 | 状态 |
|----------|------|------|------|
| S4.1 | 扩展 `get_node_edges()` 接口 | 2 | 🔲 |
| S4.2 | 实现各存储后端的过滤逻辑 | 5 | 🔲 |
| S4.3 | 编写测试用例 | 2 | 🔲 |

---

## 里程碑

| 版本 | 目标 | 预计日期 | Features |
|------|------|----------|----------|
| v1.5.0 | MVP 集成验证 | 2025-02 | Feature 1 |
| v1.6.0 | 增量更新支持 | 2025-03 | Feature 2, 3 |
| v1.7.0 | 高级查询功能 | 2025-04 | Feature 4 |

---

## 技术决策记录

### ADR-001: MVP 使用现有 API

**决策**: MVP 阶段不修改 LightRAG，LoomGraph 适配现有接口。

**原因**:
1. 现有 API 已满足核心需求
2. 减少双方开发工作量
3. 快速验证集成可行性

**约定**:
- `relation_type` 存入 `keywords` 字段
- `signature`, `language` 等额外字段拼接到 `description`
- 全量重建通过清空 `working_dir` 实现

---

## 相关文档

- [LoomGraph 需求文档](https://github.com/user/LoomGraph/docs/integration/LIGHTRAG_REQUIREMENTS.md)
- [LoomGraph 更新策略](https://github.com/user/LoomGraph/docs/architecture/UPDATE_STRATEGY.md)
- [LightRAG API 文档](../api/README.md)
